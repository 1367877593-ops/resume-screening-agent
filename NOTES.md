# 开发日志

交付要求里的「难点与解决方案」问的是**开发过程中遇到的**最大技术挑战。
事后回忆一定写得空洞，所以当场记在这里，交付时挑 2–3 条最真实的提炼进 README。

**记录规则**：踩坑当场记，别攒到晚上。每条包含四要素，缺一条就等于没记。

```markdown
### [MM-DD] 一句话现象

- **现象**：具体报错、具体错误输出，能贴原文就贴原文
- **定位**：怎么确认是这个原因的（看了 trace？二分？换模型对比？）
- **解法**：改了什么，为什么这么改而不是别的改法
- **代价 / 遗留**：引入了什么新成本，有什么没解决干净
```

同时记 prompt 的每次实质性修改（改动原因 + 观察到的效果），
这部分是 README「Prompt 设计思路」的素材来源，对应归档在 `prompts/_archive/`。

---

<!-- 开工后从这里往下追加，最新的记在最上面 -->

### [08-16] question_gen.md v1 -> v2：判据从「挂在简历上」换成「背题党能不能答」

- **现象**：三人格盲评上线后第一次跑内置样例，Q9「服务突发超时，你会怎样区分模型、
  检索和接口层的问题？」被判 `Q_NO_DISCRIMINATION` —— 背题党拿到 71 分，超过
  `bluffer_max=50`。这道题并不违反 v1 的任何一条规则。
- **定位**：v1 的规则 2 要求题目「扎在这份简历的具体内容上」，Q9 确实挂在简历里写过的
  FastAPI 上，所以顺利通过。问题出在判据本身：**「有没有引用简历内容」和「答题需不需要
  本人经历」是两件事**。分层排查是通用套路，挂谁的简历上都能问。
- **解法**：v2 把判据换成一条可证伪的自检 —— 「一个只刷过面经的人能不能答得像模像样？
  能就作废重写」，并要求每道题至少逼出具体数字口径 / 取舍理由 / 踩过的坑 / 职责边界
  四者之一。这条自检和背题党人格的评分口径是同一个，等于让出题侧直接对齐校验侧的判据。
  旧版归档在 `prompts/_archive/question_gen.v1.md`。
- **代价 / 遗留**：prompt version 变化会让缓存键全部失效，必须重跑 `make demo-cache`。
  更重要的是：**这个改动只降低发生率，不消灭问题**。内置 Demo 特意保留了 Q9 仍被判出
  的那一幕 —— 如果 prompt 写清楚就够了，就不需要校验层了。真实效果要看
  `make eval` 输出的诊断分布，不看 prompt 写得多漂亮。

### [08-16] make demo-cache 生成的缓存回放必定未命中

- **现象**：给三人格补夹具后重跑 `make demo-cache`，生成成功，但紧跟着的
  `test_demo_replay` 报 `CacheMissError`，连第一步 `jd_parse` 都没命中。
- **定位**：对比两边的缓存键。生成脚本里 `demo_mode=False`，`_stage_models()` 因此返回
  `(llm_model_cheap, llm_model)`，JD 拆解这类快阶段算出的键带的是 `deepseek-v4-flash`；
  而回放时 `demo_mode=True`，`_stage_models()` 返回 `(None, None)`，所有阶段塌缩到
  `demo-v1`。两套键不可能相交。这个缺陷从引入混合模型路由那次改动起就存在，只是此后
  没有人重跑过生成脚本，仓库里的缓存还是更早、恰好用 `demo-v1` 生成的那一批。
- **解法**：生成脚本里把 `llm_model` 和 `llm_model_cheap` 一起固定为 `demo-v1`，与回放
  的命名空间对齐。顺带修掉同一个脚本里的另一个隐患：夹具按顺序弹出，而候选人默认并行
  筛选，两个线程会交错取到对方的响应 —— 生成时强制串行，回放时并行无妨（键由内容算出）。
- **代价 / 遗留**：这类 bug 的可怕之处在于**只在评审者机器上炸**，本地只要不重跑生成脚本
  就毫无察觉。`make demo-cache` 已经把回放测试串在生成之后，以后改一次就会验一次。

### [08-14] Demo 有缓存却在命中前要求真实 API Key

- **现象**：`DEMO_MODE=1` 时，只要 `.env` 里保留了真实 provider 但 Key 为空，Harness 会在读取缓存前初始化厂商客户端并报鉴权错误。
- **定位**：沿 `call_structured()` 的执行顺序检查，发现 `get_client(settings)` 发生在缓存键计算和 `Cache.get()` 之前。
- **解法**：无显式 client 的 Demo 固定使用 `demo / demo-v1` 命名空间并延迟客户端构造；缓存命中直接返回，未命中抛 `CacheMissError`。新增测试把 provider 设为 deepseek、模型设成任意值且 Key 为空，确认全流程仍回放成功且 `get_client` 从未调用。
- **代价 / 遗留**：样例、Prompt 版本或 Schema 变化都会使缓存失效，需要运行 `make demo-cache` 显式重建；这是刻意的 fail-closed 行为。

### [08-14] 阶段 5 集成测试触发了意外的第六次模型调用

- **现象**：新加的三条端到端测试都报“脚本用尽，出现了第 6 次预期外的调用”，旧的 91 条测试全部通过。
- **定位**：检查 Reviser 收到的问题清单，确认 10 道测试题仅替换序号，题干相似度超过 `duplicate_similarity=0.85`，Checker 正确判为重复并请求修订。
- **解法**：保留查重规则和阈值，将测试夹具改为需求分析、数据治理、检索、评测、接口和监控等十个真实不同的考察点。
- **代价 / 遗留**：测试数据更长，但同时成为 UI 字段契约和题目质量的回归样本；全量测试现为 96 项通过。

### [08-14] 无 Key Demo 需要展示真实修订闭环

- **现象**：首版回放可以展示排序、题目和追问，但所有 Checker 阶段都是 0 轮修订，评审者看不到 Part B 的反馈价值。
- **定位**：浏览器逐页验收时，校验页虽然正常渲染，但无法直观看出“发现问题 → 修订 → 复检”的状态变化。
- **解法**：内置夹具首轮只返回 9 道题，让确定性数量规则报 `Q_COUNT_LT_MIN`；Reviser 补齐技术表达题后重新校验通过，UI 展示一轮修订和具体说明。
- **代价 / 遗留**：Demo 增加一个缓存条目，总计 8 个；但没有在前端伪造过程，所有步骤仍经过真实状态机与 Checker。
